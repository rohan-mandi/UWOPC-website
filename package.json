{
  "name": "uwopc-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "node-fetch": "^2.7.0",
    "dotenv": "^17.2.3",
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}